# Backend environment configuration for Notes API

# Database connection string.
# Default (if unset): sqlite:///./notes.db
# Examples:
#   SQLite file: sqlite:///./notes.db
#   Postgres:    postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME
#   MySQL:       mysql+pymysql://USER:PASSWORD@HOST:3306/DBNAME
DATABASE_URL=sqlite:///./notes.db

# CORS allowed origins for the frontend.
# Use a single origin or a comma-separated list for multiple origins.
# Examples:
#   FRONTEND_ORIGIN=http://localhost:3000
#   FRONTEND_ORIGIN=http://localhost:3000,https://myapp.example.com
# Default (if unset): *  (allow all, suitable for local development)
FRONTEND_ORIGIN=http://localhost:3000
